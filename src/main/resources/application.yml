spring:
  datasource:
    url: jdbc:mysql://localhost:3306/evolution_db
    username: root
    password: password97
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect